<nav>
    <a href="/" class="logo">Butai</a>
    <p class="logo-caption">聖地巡礼行こうぜ</p>

    <% if(typeof sidebarPartial !== "undefined" && typeof sidebarContent !== "undefined"){ %>
        <%- partial(sidebarPartial,sidebarContent) %>
    <% } else if(typeof sidebarPartial !== "undefined"){ %>
        <%- partial(sidebarPartial) %>
    <% } %>

    <div class="bottom">
        <a href="/add">投稿</a>
        <a href="/map">地図</a>
        <% if( req.user && req.user[0] ){ %>
            <a href="/u<%= req.user[0].id %>"><%= req.user[0].username %></a>
            <a href="/logout">ログアウト</a>
        <% } else { %>
            <a href="/register">登録</a>
            <a href="/login">ログイン</a>
        <% } %>
    </div>
</nav>